name: 'cross_project_target'
version: '1.0.0'
config-version: 2

model-paths: ["models"]
macro-paths: ["macros"]

models:
  cross_project_target:
    +persist_docs:
      relation: True
      columns: True



